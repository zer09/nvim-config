---
name: Project Session Init
description: What was set up during the initial session for this nvim config repo
type: project
---

During the initial session (2026-03-31), the following was established:

- Created `CLAUDE.md` at repo root documenting architecture, conventions, and environment
- Created `.memory/` at repo root as project-level memory directory
- Set up global memory at `/home/gc/.claude/projects/-home-gc--config-nvim/memory/`
- Established sync rule: all memories written to both locations
- Saved 6 feedback skills: keymap convention, plugin keymap placement, LSP server config location, no build/test step, lazy-lock.json handling, WSL2 clipboard
- Saved user language preference: "our" = user and Claude together
- Saved file read efficiency skill: use offset/limit on Read tool

**Why:** Bootstrapping shared context so future Claude instances are immediately productive in this repo.

**How to apply:** This is historical context. The actual skills are in their individual memory files.
