---
name: User OS and environment
description: User runs openSUSE Tumbleweed — use zypper for package management, not apt
type: user
---

User's OS is openSUSE Tumbleweed (rolling release). Package manager is `zypper`, not `apt`.

Example: `sudo zypper install <package>` instead of `sudo apt install <package>`.
